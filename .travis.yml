sudo: required
services: [docker]

script:
  - docker run -v $(pwd):$(pwd) -w $(pwd) snapcore/snapcraft:stable sh -c "apt update && snapcraft --version && snapcraft"
  - sudo apt -q install --yes snapd
  - sudo snap install *.snap --dangerous --classic
  - /snap/bin/powershell -v
