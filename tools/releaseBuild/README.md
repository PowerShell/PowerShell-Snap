# VSTS Release Builds

## Requirements

Docker must be installed to run any of the release builds.


## Running Linux Release Builds

From PowerShell Snap on Linux or macOS, run `.\vstsbuild.ps1 -ReleaseTag <tag> -Name <buildName>`.

Linux Build Names:

 * `powershell-snap`
     * Builds the powershell-snap
